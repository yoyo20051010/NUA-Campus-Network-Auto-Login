# 路由器部署 · 操作步骤

目标路由器：**小米 AX3000T**（Kwrt 24.10 @ `192.168.5.1`）
更新时间：2026-09-17

---

## 一、已经帮你做好的部分

| 状态 | 项目 | 结果 |
| --- | --- | --- |
| ✅ | 连接后台 | SSH `root@192.168.5.1` 正常 |
| ✅ | 安装 Python | Python 3.11.14 已装好（含 logging / codecs / urllib / openssl 全部依赖） |
| ✅ | 上传程序 | 5 个文件已放到路由器 `/tmp/openwrt` |
| ✅ | 试跑验证 | 脚本在路由器上能正常运行，门户可达、网段识别正确 |

试跑的实际结果（没有登录，只是探测）：

```
门户 HTTP 200，页面 2765 字节
门户看到的客户端地址: 10.12.66.117
应该走: 有线网段 → 先 Dr.COM 表单，失败再回退统一身份认证
账号类型: 学生账号
当前是否已在线: True
```

> 过程中发现安装脚本少了两个依赖：`python3-logging`（不然一启动就报错）和 `python3-codecs`（不然所有网络请求都会报 `unknown encoding: idna`）。这两个已经装上，也补进了 `install.sh`，下次换路由器不会再踩。

---

## 二、当前配置（2026-09-17 17:27 更新）

| 项目 | 值 |
| --- | --- |
| 校园网账号 | `M1503210`，类型 **teacher（教师账号）** |
| 检测间隔 | **15 秒**（断网后最快 15 秒开始重连） |
| 失败退避 | 教师模式**已关闭**——普通失败不退避，每 15 秒重试一次 |
| 夜间限制 | 教师模式自动忽略，整夜照常检测 |
| 在线时 | 保持静默，不刷日志 |

> 唯一的例外：遇到**账号级问题**（密码错 / 在线数超出限制 / 已在线 / 要图形验证码）会冷却 5 分钟。这类问题重试没用，而且每 15 秒撞一次有把账号撞锁的风险。想完全关掉就把 `config.json` 里 `teacher_fatal_cooldown` 改成 `0`。

> 电脑版的计划任务 `CampusNetAutoLogin` 已**禁用**（不然它会和路由器抢着登录，测试就看不出路由器行不行）。要恢复：`Enable-ScheduledTask -TaskName CampusNetAutoLogin`

---

## 三、登录/换账号（按需）

```sh
/etc/init.d/campus-net-login stop
cd /etc/campus-net-login
python3 campus_http.py --set-password          # 输入新账号 + 新密码
python3 campus_http.py --mode teacher          # 换成教师账号时必须跑这一条
/etc/init.d/campus-net-login start
```

注意 `--set-password` 会**保留**原来的账号类型，所以从学生换成教师，光改密码不够。

---

## 四、怎么确认

```sh
/etc/init.d/campus-net-login status                    # 要看到 running
tail -n 40 /etc/campus-net-login/logs/campus_http.log  # 看它做了什么
```

日志里应该出现这样的行，说明路由器自己完成了登录：

```
使用账号 M1503210（教师账号）
检测到未认证，开始尝试登录
有线网段（客户端 10.12.66.117）→ 优先走 Dr.COM 表单（可绕开统一认证的验证环节）
登录成功，网络已恢复（服务类型：校园用户(默认)）
```

跑通后，接在 LAN 口和 WiFi 上的设备直接就能上网，不用各自登录。

---

## 五、常用命令小抄

```sh
tail -f /etc/campus-net-login/logs/campus_http.log           # 实时日志
python3 /etc/campus-net-login/campus_http.py --check         # 查在线状态
python3 /etc/campus-net-login/campus_http.py --login         # 手动登录一次
python3 /etc/campus-net-login/campus_http.py --probe         # 看走哪套流程（不登录）
python3 /etc/campus-net-login/campus_http.py --mode          # 看当前账号类型
python3 /etc/campus-net-login/campus_http.py --mode teacher  # 切成教师模式
/etc/init.d/campus-net-login restart                         # 重启服务
/etc/init.d/campus-net-login stop                            # 临时停掉（想让设备单独认证时用）
```

改配置在 `/etc/campus-net-login/config.json`，改完 `restart` 生效。

---

## 六、万一出问题

| 日志里出现 | 怎么办 |
| --- | --- |
| `需要人工处理的提示 [密码]` | 账号或密码输错了，重跑 `--set-password` |
| `[已在线]` | 账号已有会话没释放，等 7~10 分钟 |
| `安全验证环节失败` | 把日志和同目录的 `http-*-captcha-page.html` 发出来 |
| `访问不到校园网门户` | 检查网线是不是插在 **WAN 口** |

服务没跑起来时，直接手动执行 `python3 /etc/campus-net-login/campus_http.py --check`，报错会打在屏幕上。
